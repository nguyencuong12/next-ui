import React from "react";
import LayoutCatProducts from "../../../components/layoutCatProducts";

const Seeds = () => {
  return <LayoutCatProducts type={4}></LayoutCatProducts>;
};

export default Seeds;
